Prompt 1
Add a restart on R key to the controller and model, adding a reset() to the model and controller calling it on R press.

This added a public void reset() to the model and made the constructor call reset() to make sure it didn't duplicate.

Prompt 2 
Add a high score 
